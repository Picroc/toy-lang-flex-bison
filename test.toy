import something;
import nothing;

class A; {
    real B;
}

public class B; {
    private int field;
    static void k(int a, int b) {
        int result;
        result = a + b;
        return result;
    }
}

class C extends B; {
    static int a(int a, int b) {
        int result;
        result = a;
        while b != 0 loop {
            a = a * a;
            b = b - 1;
        };
        return result;
    }
}